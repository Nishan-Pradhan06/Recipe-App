import 'package:flutter/material.dart';

class RecipieDescriptionScreen extends StatelessWidget {
  const RecipieDescriptionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('descripotion'),),
    );
  }
}