import 'package:flutter/material.dart';

class SizeboxGap extends StatelessWidget {
  const SizeboxGap({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
